---
// GENERATED by .tmp-echo-port.cjs from echo/index.html
// Do not hand-edit: re-run the generator instead.
---

<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Whale Echo | Conversation Intelligence for the Frontline</title>
<meta name="description" content="Echo turns frontline sales and service conversations into structured intelligence — capture, transcription, service and QC evaluation, coaching, and reporting at scale.">
<!-- Open Graph -->
<meta property="og:title" content="Whale Echo | Conversation Intelligence for the Frontline">
<meta property="og:description" content="Capture what happens on the frontline. Understand what it means. Turn every conversation into better service, better coaching, and better performance.">
<meta property="og:type" content="website">
<meta property="og:url" content="https://www.whale.sg/echo/">
<meta property="og:image" content="https://www.whale.sg/og/home.png">
<meta property="og:site_name" content="Whale">
<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="Whale Echo | Conversation Intelligence for the Frontline">
<meta name="twitter:description" content="Capture what happens on the frontline. Understand what it means. Turn every conversation into better service, better coaching, and better performance.">
<meta name="twitter:image" content="https://www.whale.sg/og/home.png">
<!-- Design system lifted from spacesight-h5; echo.css is appended last so it wins on equal specificity. -->
<link rel="stylesheet" href="/echo/echo.css">









</head>
<body>
<!-- ==========================================================================
     Site chrome — header, mobile drawer, footer, lead modal, toast.
     Markup only: taken from src/components/{Header,Footer,LeadModal}.astro.
     Their scripts are NOT repeated here — build-echo.mjs inlines the identical
     compiled copies already present in spacesight-h5/index.html, so no
     handler is ever attached twice.
     ========================================================================== -->

<header class="site-header" style="padding-top:2px;padding-bottom:2px">
  <a href="/" class="logo" aria-label="Echo for Business home" style="gap:0.8rem">
    <img src="/echo/assets/echo-for-business.png" alt="Echo for Business" height="56">
    <img src="/echo/assets/echo-icon.png" alt="" height="56">
  </a>
  <button type="button" class="btn btn-primary header-cta" data-open-modal data-source-type="spacesight2页-顶栏留资">Book a Demo</button>
</header>

<!-- ==========================================================================
     Echo body content. Copy is transcribed from 页面草稿初版.pptx (13 slides).
     Section ids double as the vertical tab-rail anchors; the DATA-TAB values
     are what the scroll-spy in the shared script matches against.
     ========================================================================== -->
<main class="ss2" id="top">

  <!-- ===================== COVER · slide 01 ===================== -->
  <section class="section ss2-cover ss2-part" id="cover" data-tab="cover" aria-labelledby="cover-h">
    <div class="container">

      <div class="ss2-cover-grid" style="display:block">
        <div class="ss2-cover-copy">
          <p class="ec-kicker">Conversation Intelligence</p>
          <h1 class="ss2-h1" id="cover-h"><span class="wm"><span class="w" style="--i:0">Turn</span></span> <span class="wm"><span class="w" style="--i:1">Frontline</span></span><br> <span class="wm"><span class="w" style="--i:2">Conversations</span></span> <span class="wm"><span class="w" style="--i:3">Into</span></span><br> <span class="wm"><span class="w" style="--i:4">Actionable</span></span> <span class="ss2-tail"><span class="wm"><span class="w" style="--i:5">Intelligence</span></span></span></h1>
          <p class="ss2-body">Echo captures every frontline conversation, uncovers what matters, and transforms real-world interactions into better service, smarter coaching, and stronger business performance.</p>
          <div class="ss2-cover-cta">
            <button type="button" class="btn btn-primary btn-large" data-open-modal data-source-type="Echo 落地页 — 首屏">Design an Echo Pilot</button>
            <a class="btn btn-ghost" href="#usecases">See how it works</a>
          </div>
        </div>
      </div>

    </div>
    <div class="ss2-cover-media">
      <div class="ss2-cover-visual" aria-hidden="true">
        <picture class="ss2-cover-bg">
          <source srcset="/echo/assets/echo-hero-mobile.png" media="(max-width: 720px)">
          <!-- PC/pad hero — this `img` is the >720px branch, so it is what pads at
              768px+ get; at <=720px the <source> above takes over. Art is
              素材/echo 2.jpg/echo.jpg (2560x1278): the front-desk group with the
              Echo UI panel, and the three device cards in rounded tiles along the
              bottom. Re-export over assets/echo-cover-pc.jpg to change it. -->
          <img src="/echo/assets/echo-cover-pc.jpg" alt="" loading="eager">
        </picture>
        <img class="ss2-cover-float ss2-cover-float--right" src="/echo/assets/hero-float-right.png" alt="">
        <img class="ss2-cover-float ss2-cover-float--bottom" src="/echo/assets/hero-float-bottom.png" alt="">
      </div>
      <div class="ss2-cover-cta ss2-cover-cta--after-visual">
        <button type="button" class="btn btn-primary btn-large" data-open-modal data-source-type="Echo 落地页 — 首屏">Design an Echo Pilot</button>
        <a class="btn btn-ghost" href="#usecases">See how it works</a>
      </div>
    </div>
  </section>

  <!-- ===================== PROOF STRIP · cover stats ===================== -->
  <section class="section ss2-part ec-proof-strip">
    <div class="container">
      <dl class="ss2-proof" data-reveal>
        <div class="ss2-proof-cell"><dt>Languages, dialects &amp; accented speech</dt><dd>30<span class="ss2-proof-plus">+</span></dd></div>
        <div class="ss2-proof-cell"><dt>Transcription accuracy <span class="ec-hedge">(up to)</span></dt><dd>95<span class="ss2-proof-plus">%</span></dd></div>
        <div class="ss2-proof-cell"><dt>Hours of sales conversations analysed monthly <span class="ec-hedge">(up to)</span></dt><dd>60,000</dd></div>
      </dl>
    </div>
  </section>

  <!-- ===================== PART 01 · WHY ECHO · slide 02 ===================== -->
  <section class="section section-tint ss2-part" id="why" data-tab="why" aria-labelledby="why-h">
    <div class="container">
      <!-- Headline and figure share a row so the plate's top edge lands on the
           headline's top edge and its bottom edge on the ledger's last rule. -->
      <div class="ss2-vis-grid ec-why">
        <div class="ec-why-col">
          <h2 class="ss2-h2" id="why-h" data-reveal>Frontline signals rarely reach management.</h2>
          <p class="ss2-body" data-reveal style="--i:1">Store managers can't see most frontline sales conversations — so important interactions go unrecorded, and patterns stay hidden.</p>

          <div class="ss2-ledger ss2-ledger-trust" data-reveal style="--i:2">
            <p class="ss2-ledger-head"><span>Barrier</span><span>What it costs</span></p>
            <div class="ss2-ledger-row"><span class="ss2-ledger-name">Limited visibility</span><span class="ss2-ledger-desc">Managers cannot listen to every conversation — so important patterns stay hidden.</span></div>
            <div class="ss2-ledger-row"><span class="ss2-ledger-name">Manual QC</span><span class="ss2-ledger-desc">Reviewing recordings one by one is slow, inconsistent, and difficult to scale.</span></div>
            <div class="ss2-ledger-row"><span class="ss2-ledger-name">Delayed action</span><span class="ss2-ledger-desc">By the time a recurring service issue becomes visible, the opportunity to improve may be gone.</span></div>
          </div>
        </div>

        <figure class="ss2-plate" data-reveal style="--i:3">
          <!-- Art is a crop of 素材/6056090f-07c3-4f0a-88a6-b328807b54e3.jfif.
               That file is the finished 1280x720 deck slide, so it carries the
               eyebrow, this headline, the body copy and the three-barrier ledger
               baked into the pixels, plus a "Sample only" watermark — dropping it
               in whole would print the headline twice and put the watermark on the
               live page. Cropped to the picture only (x527 w753) with the ledger
               card's right edge painted out and the watermark removed by delogo. -->
          <div class="ss2-plate-media"><img src="/echo/assets/echo-why.jpg" alt="Store manager reviewing frontline conversation data on a dashboard" width="752" height="720" loading="lazy"></div>
        </figure>
      </div>
    </div>
  </section>

  <!-- ===================== PART 02 · THE ECHO APPROACH · slide 03 ===================== -->
  <section class="section ss2-part" id="approach" data-tab="approach" aria-labelledby="approach-h">
    <div class="container">
      <header class="ss2-part-head" data-reveal>
        <h2 class="ss2-h2" id="approach-h">From conversation to <span class="ec-blue">continuous improvement.</span></h2>
        <p class="ss2-body">Echo transforms real-world frontline conversations into structured intelligence — helping teams understand customers, evaluate service quality, and turn insights into action.</p>
      </header>

      <ol class="ss2-steps" data-reveal>
        <li class="ss2-step"><span class="ss2-step-no">Step 01</span><span class="ss2-step-name">Capture</span><p class="ss2-step-body">Record the conversations that matter with a mobile device that connects via 4G or Bluetooth and fits naturally into your team’s existing workflows.</p></li>
        <li class="ss2-step"><span class="ss2-step-no">Step 02</span><span class="ss2-step-name">Understand</span><p class="ss2-step-body">Turn audio into searchable intelligence through transcription and structured conversation outputs.</p></li>
        <li class="ss2-step"><span class="ss2-step-no">Step 03</span><span class="ss2-step-name">Evaluate</span><p class="ss2-step-body">Measure observable service behaviors against client-specific service and quality-control criteria.</p></li>
        <li class="ss2-step"><span class="ss2-step-no">Step 04</span><span class="ss2-step-name">Coach</span><p class="ss2-step-body">Surface the conversations, moments, and patterns that matter—giving managers clear evidence for targeted feedback.</p></li>
        <li class="ss2-step"><span class="ss2-step-no">Step 05</span><span class="ss2-step-name">Improve</span><p class="ss2-step-body">Identify patterns across teams and locations, and turn insights into continuous improvement.</p></li>
      </ol>

      <figure class="ss2-plate ec-wide" data-reveal>
        <div class="ss2-plate-media ss2-fig-media"><video src="https://whale-web.meetwhale.com/video/Echo_Video_Demo.mp4" autoplay muted loop playsinline width="1600" height="1016" aria-label="Echo application demo" style="width:100%;height:auto;display:block"></video></div>
      </figure>
    </div>
  </section>

  <!-- Languages capabilities -->
  
  <section class="section ec-lang-section" id="languages" data-reveal>
    <div class="container">
      <div class="ec-lang-grid">
        <div class="ec-lang-copy">
          <h2 class="ss2-h2">30+ Languages <span class="ec-blue">Global teams</span> Instant understanding</h2>
          <ul class="ec-lang-list">
            <li>Recognizes 30+ languages, dialects, and accented speech</li>
            <li>Real-time translation for cross-border communication</li>
            <li>Industry vocabulary support for specialized scenarios</li>
          </ul>
        </div>
        <div class="ec-lang-media">
          <img src="/echo/assets/lang-feature.jpg" alt="Language capabilities illustration" width="1200" height="800" loading="lazy">
        </div>
      </div>
    </div>
  </section>

  <!-- Echo Wearables -->
  <section class="section ec-lang-section" id="wearables" data-reveal>
    <div class="container">
      <div class="ec-lang-grid">
        <div class="ec-lang-media">
          <img src="/echo/assets/wearables-feature.jpg" alt="Echo Wearables illustration" width="1200" height="800" loading="lazy">
        </div>
        <div class="ec-lang-copy">
          <h2 class="ss2-h2">Echo Wearables<br><span class="ec-blue">Real-time AI coaching, right in your ear</span></h2>
          <ul class="ec-lang-list">
            <li>Live audio capture with AI noise cancellation</li>
            <li>One-tap command relay without opening your phone</li>
            <li>All-day battery for frontline shifts and attendance tracking</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Life Mode -->
  <section class="section ec-lang-section" id="lifemode" data-reveal>
    <div class="container">
      <div class="ec-lang-grid">
        <div class="ec-lang-copy">
          <h2 class="ss2-h2">Life Mode<br><span class="ec-blue">Turn every conversation into reusable knowledge.</span></h2>
          <ul class="ec-lang-list">
            <li>Automatically organizes recordings into structured records</li>
            <li>Extracts pain points, risks, and opportunities into reports</li>
            <li>Builds a searchable voice knowledge base for the whole team</li>
          </ul>
        </div>
        <div class="ec-lang-media">
          <img src="/echo/assets/lifemode-feature.jpg" alt="Life Mode illustration" width="1200" height="800" loading="lazy">
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== PART 04 · USE CASES · slide 05 ===================== -->
  <section class="section ss2-part" id="usecases" data-tab="usecases" aria-labelledby="usecases-h">
    <div class="container">
      <header class="ss2-part-head" data-reveal>
        <h2 class="ss2-h2" id="usecases-h">One intelligence layer. <span class="ec-blue">Many frontline moments.</span></h2>
        <p class="ss2-body">The same capture-to-insight loop applies wherever your teams talk to customers — on the floor, at the counter, or across a service desk.</p>
      </header>

      <div class="ec-scene-carousel" data-scene-carousel data-reveal>
        <div class="ec-scene-carousel-track-wrapper">
          <div class="ec-scene-carousel-track" role="list">
            <figure class="ec-scene-carousel-slide" role="listitem" aria-label="Retail Sales">
              <img src="/echo/assets/usecases-carousel/retail-sales.jpg" alt="Retail Sales use case: a store associate helping a customer choose a laptop, alongside the conversation transcript and an Echo AI insight panel listing customer need detected, product interest, and next best action." width="1920" height="1080" loading="lazy">
            </figure>
            <figure class="ec-scene-carousel-slide" role="listitem" aria-label="Training and Coaching">
              <img src="/echo/assets/usecases-carousel/training-coaching.jpg" alt="Training and Coaching use case: a manager reviewing team skill progress and recommended coaching actions on a laptop, alongside an Echo AI insight panel listing coaching opportunity, best practice, and skill gap." width="1920" height="1080" loading="lazy">
            </figure>
            <figure class="ec-scene-carousel-slide" role="listitem" aria-label="Hospitality">
              <img src="/echo/assets/usecases-carousel/hospitality.jpg" alt="Hospitality use case: a hotel receptionist handling a late-checkout request at the front desk, alongside the conversation transcript and an Echo AI insight panel listing guest preference, service issue, and follow-up needed." width="1920" height="1080" loading="lazy">
            </figure>
            <figure class="ec-scene-carousel-slide" role="listitem" aria-label="Field Operations">
              <img src="/echo/assets/usecases-carousel/field-operations.jpg" alt="Field Operations use case: two store staff discussing a low-stock shelf on the shop floor, alongside the conversation transcript and an Echo AI insight panel listing stock issue, process gap, and escalate to manager." width="1920" height="1080" loading="lazy">
            </figure>
            <figure class="ec-scene-carousel-slide" role="listitem" aria-label="Customer Service">
              <img src="/echo/assets/usecases-carousel/customer-service.jpg" alt="Customer Service use case: a support agent helping a customer whose order is delayed, alongside the conversation transcript and an Echo AI insight panel listing issue summary, sentiment risk, and resolution coaching." width="1920" height="1080" loading="lazy">
            </figure>
          </div>
        </div>
        <button type="button" class="ec-scene-carousel-prev" aria-label="Previous use case">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m15 18-6-6 6-6"/></svg>
        </button>
        <button type="button" class="ec-scene-carousel-next" aria-label="Next use case">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m9 18 6-6-6-6"/></svg>
        </button>
        <div class="ec-scene-carousel-dots" role="tablist" aria-label="Use case slides">
          <button type="button" class="ec-scene-carousel-dot is-active" role="tab" aria-label="Slide 1 — Retail Sales" aria-selected="true"></button>
          <button type="button" class="ec-scene-carousel-dot" role="tab" aria-label="Slide 2 — Training and Coaching" aria-selected="false"></button>
          <button type="button" class="ec-scene-carousel-dot" role="tab" aria-label="Slide 3 — Hospitality" aria-selected="false"></button>
          <button type="button" class="ec-scene-carousel-dot" role="tab" aria-label="Slide 4 — Field Operations" aria-selected="false"></button>
          <button type="button" class="ec-scene-carousel-dot" role="tab" aria-label="Slide 5 — Customer Service" aria-selected="false"></button>
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== PART 05 · DEPLOYMENT · slide 09 ===================== -->
  <section class="section section-tint ss2-part" id="deploy" data-tab="deploy" aria-labelledby="deploy-h">
    <div class="container">
      <header class="ss2-part-head" data-reveal>
        <h2 class="ss2-h2" id="deploy-h">Flexible deployment options.</h2>
        <p class="ss2-body">Three capture form factors, one intelligence layer. Choose per site, per team, or per shift.</p>
      </header>

      <div class="ss2-hw-cards ec-dev">

        <article class="ss2-hw-card" data-reveal>
          <div class="ss2-hw-head">
            <span class="ss2-hw-ic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="8" y="3" width="8" height="18" rx="3"/><path d="M12 17.5h.01"/></svg></span>
            <h3 class="ss2-h3">Echo Mini</h3>
          </div>
          <div class="ss2-hw-gallery"><img src="/echo/assets/dev-mini.jpg" alt="Echo Mini, a compact capture device for fixed service counters" width="333" height="697" loading="lazy"></div>
          <p>For fixed service counters and single-station capture.</p>
          <div class="ss2-hw-ideal">
            <p class="ss2-hw-ideal-label">Typical setup</p>
            <ul>
              <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>Counter &amp; checkout</li>
              <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>Service desk</li>
              <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>Single fixed station</li>
            </ul>
          </div>
          <div class="ss2-hw-cta"><button type="button" class="btn" data-open-modal data-source-type="Echo 部署方案咨询 — Echo Mini">Learn more →</button></div>
        </article>

        <article class="ss2-hw-card" data-reveal style="--i:1">
          <div class="ss2-hw-head">
            <span class="ss2-hw-ic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="5" y="4" width="14" height="16" rx="3"/><path d="M9 9h6M9 13h6"/></svg></span>
            <h3 class="ss2-h3">Echo Pro</h3>
          </div>
          <div class="ss2-hw-gallery"><img src="/echo/assets/dev-pro.jpg" alt="Echo Pro, a wearable capture device for mobile associates" width="499" height="697" loading="lazy"></div>
          <p>For mobile associates covering the whole floor.</p>
          <div class="ss2-hw-ideal">
            <p class="ss2-hw-ideal-label">Typical setup</p>
            <ul>
              <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>Sales floor</li>
              <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>Full-shift coverage</li>
              <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>Mobile teams</li>
            </ul>
          </div>
          <div class="ss2-hw-cta"><button type="button" class="btn" data-open-modal data-source-type="Echo 部署方案咨询 — Echo Pro">Learn more →</button></div>
        </article>

        <article class="ss2-hw-card" data-reveal style="--i:2">
          <div class="ss2-hw-head">
            <span class="ss2-hw-ic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><circle cx="7.5" cy="9" r="3.5"/><path d="M7.5 12.5V20"/><circle cx="16.5" cy="9" r="3.5"/><path d="M16.5 12.5V20"/></svg></span>
            <h3 class="ss2-h3">Echo Buds</h3>
          </div>
          <div class="ss2-hw-gallery"><img src="/echo/assets/dev-buds.jpg" alt="Echo Buds, an earbud case and earbud for hands-free capture" width="745" height="697" loading="lazy"></div>
          <p>For hands-free capture and live coaching in the ear.</p>
          <div class="ss2-hw-ideal">
            <p class="ss2-hw-ideal-label">Typical setup</p>
            <ul>
              <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>Hands-free capture</li>
              <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>Live coaching</li>
              <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6L9 17l-5-5"/></svg>Noisy environments</li>
            </ul>
          </div>
          <div class="ss2-hw-cta"><button type="button" class="btn" data-open-modal data-source-type="Echo 部署方案咨询 — Echo Buds">Learn more →</button></div>
        </article>

      </div>
    </div>
  </section>

  <!-- ===================== PART 06 · PROVEN AT SCALE · slides 10, 11, 12 ===================== -->
  <section class="section ss2-part" id="cases" data-tab="cases" aria-labelledby="cases-h">
    <div class="container">
      <header class="ss2-part-head" data-reveal>
        <h2 class="ss2-h2" id="cases-h">Deployed on real frontline conversations.</h2>
      </header>

      <div class="case-grid ss2-case-grid">

        <article class="case-card" data-reveal>
          <div class="case-visual"><img src="/echo/assets/case-ocbc.jpg" alt="OCBC" width="1600" height="1609" loading="lazy"></div>
          <div class="case-body">
            <div class="ss2-case-meta"><span class="ss2-case-brand">Echo × OCBC</span></div>
            <h3 class="ss2-case-title">Credit-card sales conversations, reviewed at scale.</h3>
            <p class="case-industry">Banking &amp; Financial Services</p>
            <ul class="ec-stats">
              <li class="ec-stat"><span class="ec-stat-v">+150%</span><span class="ec-stat-l">Sales productivity</span></li>
              <li class="ec-stat"><span class="ec-stat-v">+80%</span><span class="ec-stat-l">Serving capability</span></li>
              <li class="ec-stat"><span class="ec-stat-v">+20%</span><span class="ec-stat-l">Conversion rate</span></li>
            </ul>
          </div>
        </article>

        <article class="case-card" data-reveal style="--i:1">
          <div class="case-visual"><img src="/echo/assets/case-byd.jpg" alt="BYD" width="1600" height="1728" loading="lazy"></div>
          <div class="case-body">
            <div class="ss2-case-meta"><span class="ss2-case-brand">Echo × BYD</span></div>
            <h3 class="ss2-case-title">EV retail sales conversations, captured and analysed monthly.</h3>
            <p class="case-industry">Automotive</p>
            <ul class="ec-stats">
              <!-- the deck states both of these as "Up to …" for this deployment,
                   so the hedge stays attached to the figure at its source too -->
              <li class="ec-stat"><span class="ec-stat-v">60,000</span><span class="ec-stat-l">Hours captured &amp; analysed each month <span class="ec-hedge">(up to)</span></span></li>
              <li class="ec-stat"><span class="ec-stat-v">95%</span><span class="ec-stat-l">Transcription accuracy <span class="ec-hedge">(up to)</span></span></li>
              <li class="ec-stat"><span class="ec-stat-v">+5%</span><span class="ec-stat-l">Service improvement</span></li>
            </ul>
          </div>
        </article>

        <article class="case-card" data-reveal style="--i:2">
          <div class="case-visual"><img src="/echo/assets/case-daikin.jpg" alt="Daikin" width="1425" height="1652" loading="lazy"></div>
          <div class="case-body">
            <div class="ss2-case-meta"><span class="ss2-case-brand">Echo × Daikin</span></div>
            <h3 class="ss2-case-title">Sales conversations turned into structured reports.</h3>
            <p class="case-industry">HVAC &amp; Manufacturing</p>
            <ul class="ec-stats">
              <li class="ec-stat"><span class="ec-stat-v">+70%</span><span class="ec-stat-l">Sales productivity</span></li>
              <li class="ec-stat"><span class="ec-stat-v">+10%</span><span class="ec-stat-l">Conversion rate</span></li>
              <li class="ec-stat"><span class="ec-stat-v">+4%</span><span class="ec-stat-l">New customer conversion rate</span></li>
            </ul>
          </div>
        </article>

      </div>
    </div>
  </section>

  <!-- ===================== FAQ · slide 13 ===================== -->
  <div data-tab="faq">
    <section class="section section-tint" id="faq" aria-labelledby="faq-h">
      <div class="container">
        <div class="section-head center" data-reveal>
          <h2 class="hero-shimmer" id="faq-h">Frequently Asked Questions</h2>
          <p>What teams usually ask before an Echo pilot.</p>
        </div>

        <div class="faq-list">
          <details class="faq-item" name="faq">
            <summary class="faq-question"><span class="faq-number">01</span><span class="faq-text">What is Echo?</span><span class="faq-icon" aria-hidden="true"></span></summary>
            <div class="faq-answer"><p>Echo is Whale's conversation-intelligence layer for sales and service. It captures supported conversations, transcribes and structures them, evaluates selected service or QC criteria, and produces summaries, coaching, customer insights, and reports.</p></div>
          </details>

          <details class="faq-item" name="faq">
            <summary class="faq-question"><span class="faq-number">02</span><span class="faq-text">Can Echo work in noisy stores, hotels, or service counters?</span><span class="faq-icon" aria-hidden="true"></span></summary>
            <div class="faq-answer"><p>Yes. Echo Buds apply AI noise cancellation at the point of capture, and audio is picked up close to the speaker rather than from a room microphone. That keeps recognition stable in busy environments — including the dialects and accented speech common across global teams.</p></div>
          </details>

          <details class="faq-item" name="faq">
            <summary class="faq-question"><span class="faq-number">03</span><span class="faq-text">Can Echo evaluate our own SOP or compliance script?</span><span class="faq-icon" aria-hidden="true"></span></summary>
            <div class="faq-answer"><p>Yes. Evaluation criteria are client-specific: your service standards, SOP steps, and compliance scripts are configured as the criteria that Echo measures observable behaviors against — so reviews reflect your operating model rather than a generic scorecard.</p></div>
          </details>

          <details class="faq-item" name="faq">
            <summary class="faq-question"><span class="faq-number">04</span><span class="faq-text">How are privacy and consent handled?</span><span class="faq-icon" aria-hidden="true"></span></summary>
            <div class="faq-answer"><p>Echo is deployed with the consent and privacy requirements defined by your organisation and jurisdiction. Capture scope, retention, access rights, and which conversations are eligible for review are configured per deployment, and every configuration is agreed before rollout.</p></div>
          </details>

          <details class="faq-item" name="faq">
            <summary class="faq-question"><span class="faq-number">05</span><span class="faq-text">How is Echo ROI measured?</span><span class="faq-icon" aria-hidden="true"></span></summary>
            <div class="faq-answer"><p>Echo is scoped as a measurable pilot. Agreed baseline metrics — sales productivity, conversion rate, service-quality scores, or review coverage — are compared before and after rollout, using the same reporting Echo produces from recorded conversations.</p></div>
          </details>
        </div>
      </div>
    </section>
  </div>

  <!-- ===================== CLOSING CTA · slide 01 ===================== -->
  <section class="section section-dark cta-section ec-cta" id="contact" data-tab="contact" aria-labelledby="contact-h">
    <div class="container cta-container">
      <div class="cta-layout">
        <div class="cta-copy">
          <h2 id="contact-h">Design an Echo Pilot</h2>
          <p class="cta-lead">Start with one site, one team, and the conversations that already happen every day. Together we'll scope the capture setup, the evaluation criteria, and the metrics that define success.</p>
          <div class="cta-actions">
            <button type="button" class="btn btn-primary btn-large" data-open-modal data-source-type="spacesight2页-底部留资">Book a Demo</button>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== vertical section rail ===================== -->
  <nav class="ss2-tabs" aria-label="Page sections">
    <a href="#cover" class="ss2-tab" data-tab-link="cover">COVER</a>
    <a href="#why" class="ss2-tab" data-tab-link="why">WHY</a>
    <a href="#approach" class="ss2-tab" data-tab-link="approach">APPROACH</a>
    <a href="#usecases" class="ss2-tab" data-tab-link="usecases">USE CASES</a>
    <a href="#deploy" class="ss2-tab" data-tab-link="deploy">DEPLOY</a>
    <a href="#cases" class="ss2-tab" data-tab-link="cases">CASES</a>
    <a href="#faq" class="ss2-tab" data-tab-link="faq">FAQ</a>
    <a href="#contact" class="ss2-tab" data-tab-link="contact">CONTACT</a>
  </nav>

</main>



<footer class="site-footer" id="footer-contact">
  <div class="container">
    <div class="footer-grid">
      <div class="footer-brand-left">
        <a href="https://www.meetwhale.ai/" class="logo" target="_blank" rel="noopener"><img src="/echo/assets/footer-logo.png" alt="Whale" height="32"></a>
        <p>AI that powers enterprise operations across physical and digital channels.</p>
      </div>
      <div class="footer-brand-right">
          <div class="footer-contact">
          <h4>Contact Us</h4>
          <p><a href="mailto:whaleglobal@whale.im">whaleglobal@whale.im</a></p>
          <p class="footer-regions">
            <a href="https://meetwhale.ai">United States</a> |
            <a href="https://www.whale.sg/">Singapore</a> |
            <a href="https://www.meetwhale.com/">China</a> |
            <a href="https://www.whaleai.co.jp/">Japan</a>
          </p>
        </div>
        <div class="footer-social">
          <h4>Follow Us</h4>
          <div class="social-links">
            <a href="https://www.facebook.com/Whale-105271425542708" aria-label="Facebook">
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>
            </a>
            <a href="https://www.linkedin.com/company/hellowhale" aria-label="LinkedIn">
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
            </a>
          </div>
        </div>
      </div>
    </div>
    <div class="footer-bottom">
      <span>© 2026 WHALE FUTURE INC.</span>
      <div class="footer-legal">
        <a href="/legal/">Legal</a>
        <a href="/privacy/">Privacy</a>
      </div>
      <div class="footer-lang" data-lang-dropdown>
        <button type="button" class="lang-toggle" aria-expanded="false" aria-haspopup="listbox">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
            <circle cx="12" cy="12" r="10"/>
            <path d="M2 12h20M12 2a15.3 15.3 0 014 10 15.3 15.3 0 01-4 10 15.3 15.3 0 01-4-10 15.3 15.3 0 014-10z"/>
          </svg>
          <span>English (US)</span>
          <svg class="chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
            <path d="M6 9l6 6 6-6"/>
          </svg>
        </button>
        <ul class="lang-menu" role="listbox">
          <li><a href="https://www.meetwhale.com/" target="_blank" rel="noopener noreferrer" role="option">Chinese (简体中文)</a></li>
          <li><a href="https://www.whaleai.co.jp/" target="_blank" rel="noopener noreferrer" role="option">Japanese (日本語)</a></li>
          <li><a href="https://www.whale.sg/" target="_blank" rel="noopener noreferrer" role="option" aria-current="true">English (Singapore)</a></li>
        </ul>
      </div>
    </div>
  </div>
</footer>

<div class="lead-modal" id="leadModal" role="dialog" aria-modal="true" aria-labelledby="leadModalTitle" hidden>
  <div class="lead-modal-backdrop" data-close-modal></div>
  <div class="lead-modal-panel">
    <button type="button" class="lead-modal-close" data-close-modal aria-label="Close">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <line x1="18" y1="6" x2="6" y2="18"/>
        <line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
    </button>

    <div class="lead-modal-header">
      <h3 id="leadModalTitle">Request a Demo</h3>
      <p class="lead-modal-subtitle">Fill in your details and our team will reach out within one business day.</p>
    </div>

    <form class="lead-form" data-lead-form novalidate>
      <input type="hidden" name="unionCode" value="">
      <input type="hidden" name="sourceType" id="leadModalSourceType" value="美区官网留资">
      <input type="hidden" name="sourceDesc" id="leadModalSourceDesc" value="">

      <div class="lead-field">
        <label for="lead-company">Company <span aria-label="required">*</span></label>
        <input id="lead-company" type="text" name="customerName" placeholder="Your company name" required>
      </div>

      <div class="lead-field">
        <label for="lead-name">Full Name <span aria-label="required">*</span></label>
        <input id="lead-name" type="text" name="contactName" placeholder="Your name" required>
      </div>

      <div class="lead-field">
        <label for="lead-country">Country <span aria-label="required">*</span></label>
        <select id="lead-country" name="customerCountryCode" required>
          <option value="583542" selected>United States</option>
          <option value="583560">Canada</option>
          <option value="4744">中国</option>
          <option value="583547">香港</option>
          <option value="583548">台灣</option>
          <option value="583541">Singapore</option>
          <option value="583544">日本</option>
          <option value="583545">대한민국</option>
          <option value="583546">Malaysia</option>
          <option value="583561">United Kingdom</option>
          <option value="583558">Deutschland</option>
          <option value="583557">Australia</option>
          <option value="583566">New Zealand</option>
        </select>
      </div>

      <div class="lead-field">
        <label for="lead-phone">Phone <span aria-label="required">*</span></label>
        <input id="lead-phone" type="tel" name="contactPhone" placeholder="+1 234 567 8900" required>
      </div>

      <div class="lead-field">
        <label for="lead-email">Work Email <span aria-label="required">*</span></label>
        <input id="lead-email" type="email" name="contactEmail" placeholder="you@company.com" required>
      </div>

      <div class="lead-field">
        <label for="lead-message">Message <span class="lead-optional">(optional)</span></label>
        <textarea id="lead-message" name="remark" placeholder="Tell us about your project or questions..." rows="3"></textarea>
      </div>

      <button type="submit" class="btn btn-primary btn-large lead-submit">Submit</button>

      <p class="lead-form-note">By submitting, you agree to our privacy policy.</p>
    </form>

    <p class="lead-error" hidden></p>
  </div>
</div>

<div class="lead-toast" id="leadToast" role="status" aria-live="polite" hidden>
  <div class="lead-toast-icon" aria-hidden="true">✓</div>
  <div class="lead-toast-content">
    <strong>Thank you!</strong>
    <span>We've received your request and will be in touch shortly.</span>
  </div>
</div>


<script is:inline>
(function () {
  'use strict';
  var toggle = document.querySelector('.nav-toggle');
  var drawer = document.getElementById('mobile-nav-drawer');
  var backdrop = document.querySelector('.nav-drawer-backdrop');
  if (!toggle || !drawer || !backdrop) return;

  var lastFocused = null;
  var lockedScrollY = 0;

  function focusables() {
    return drawer.querySelectorAll('a[href], button:not([disabled])');
  }
  function lockScroll() {
    lockedScrollY = window.scrollY;
    document.body.style.overflow = 'hidden';   /* mirrors leads.js openModal */
    document.body.style.position = 'fixed';    /* iOS Safari body-lock */
    document.body.style.top = '-' + lockedScrollY + 'px';
    document.body.style.left = '0';
    document.body.style.right = '0';
  }
  function unlockScroll() {
    document.body.style.overflow = '';
    document.body.style.position = '';
    document.body.style.top = '';
    document.body.style.left = '';
    document.body.style.right = '';
    window.scrollTo(0, lockedScrollY);
  }
  function openDrawer() {
    lastFocused = document.activeElement;
    drawer.classList.add('is-open');
    backdrop.classList.add('is-open');
    toggle.setAttribute('aria-expanded', 'true');
    lockScroll();
    var first = focusables()[0];
    if (first) first.focus();
  }
  function closeDrawer() {
    drawer.classList.remove('is-open');
    backdrop.classList.remove('is-open');
    toggle.setAttribute('aria-expanded', 'false');
    unlockScroll();
    if (lastFocused && document.contains(lastFocused)) lastFocused.focus();
  }
  function isOpen() { return drawer.classList.contains('is-open'); }

  toggle.addEventListener('click', function () { isOpen() ? closeDrawer() : openDrawer(); });
  backdrop.addEventListener('click', closeDrawer);
  drawer.querySelectorAll('[data-nav-close]').forEach(function (el) {
    el.addEventListener('click', closeDrawer);
  });
  /* close on link click and on drawer CTA (leads.js opens modal after this handler) */
  drawer.querySelectorAll('a, [data-open-modal]').forEach(function (el) {
    el.addEventListener('click', closeDrawer);
  });

  document.addEventListener('keydown', function (e) {
    if (!isOpen()) return;
    if (e.key === 'Escape') { closeDrawer(); return; }
    if (e.key === 'Tab') {  /* minimal focus trap */
      var items = Array.prototype.slice.call(focusables());
      if (!items.length) return;
      var first = items[0], last = items[items.length - 1];
      if (e.shiftKey && document.activeElement === first) { e.preventDefault(); last.focus(); }
      else if (!e.shiftKey && document.activeElement === last) { e.preventDefault(); first.focus(); }
    }
  });

  /* safety: never leave the drawer open across the breakpoint */
  window.matchMedia('(min-width: 721px)').addEventListener('change', function (e) {
    if (e.matches && isOpen()) closeDrawer();
  });
})();
</script>
<script is:inline>
/**
 * Use-case scenario carousel (PART 04). Slides advance on their own, and stop
 * for hover, focus, or a hidden tab so the motion never fights the reader.
 */
(function () {
  'use strict';

  var carousel = document.querySelector('[data-scene-carousel]');
  if (!carousel) return;

  var track = carousel.querySelector('.ec-scene-carousel-track');
  var slides = Array.prototype.slice.call(carousel.querySelectorAll('.ec-scene-carousel-slide'));
  var prevBtn = carousel.querySelector('.ec-scene-carousel-prev');
  var nextBtn = carousel.querySelector('.ec-scene-carousel-next');
  var dots = Array.prototype.slice.call(carousel.querySelectorAll('.ec-scene-carousel-dot'));
  if (!track || slides.length < 2) return;

  var current = 0;
  var total = slides.length;
  var autoplayTimer = null;
  var AUTOPLAY_DELAY = 5000;

  function update() {
    track.style.transform = 'translateX(' + (-current * 100) + '%)';
    dots.forEach(function (dot, i) {
      var active = i === current;
      dot.classList.toggle('is-active', active);
      dot.setAttribute('aria-selected', active ? 'true' : 'false');
    });
    slides.forEach(function (slide, i) {
      slide.setAttribute('aria-hidden', i !== current ? 'true' : 'false');
    });
  }

  function goTo(index) {
    current = (index + total) % total;
    update();
  }

  function next() { goTo(current + 1); }
  function prev() { goTo(current - 1); }

  function startAutoplay() {
    stopAutoplay();
    autoplayTimer = setInterval(next, AUTOPLAY_DELAY);
  }
  function stopAutoplay() {
    if (autoplayTimer) { clearInterval(autoplayTimer); autoplayTimer = null; }
  }

  if (prevBtn) prevBtn.addEventListener('click', function () { stopAutoplay(); prev(); startAutoplay(); });
  if (nextBtn) nextBtn.addEventListener('click', function () { stopAutoplay(); next(); startAutoplay(); });

  dots.forEach(function (dot, i) {
    dot.addEventListener('click', function () { stopAutoplay(); goTo(i); startAutoplay(); });
  });

  carousel.addEventListener('mouseenter', stopAutoplay);
  carousel.addEventListener('mouseleave', startAutoplay);
  carousel.addEventListener('focusin', stopAutoplay);
  carousel.addEventListener('focusout', startAutoplay);

  carousel.addEventListener('keydown', function (e) {
    if (e.key === 'ArrowLeft') { e.preventDefault(); stopAutoplay(); prev(); startAutoplay(); }
    else if (e.key === 'ArrowRight') { e.preventDefault(); stopAutoplay(); next(); startAutoplay(); }
  });

  document.addEventListener('visibilitychange', function () {
    if (document.hidden) stopAutoplay(); else startAutoplay();
  });

  // The slides sit side by side off-screen, so `loading="lazy"` holds them back
  // until the moment one actually slides in — which shows a blank frame on the
  // deck's first pass. Warm all five once the page itself has finished loading,
  // so a slide is never painted before its artwork has arrived.
  function warmSlides() {
    slides.forEach(function (slide) {
      var img = slide.querySelector('img');
      if (!img) return;
      var warm = new Image();
      warm.src = img.getAttribute('src');
    });
  }
  if (document.readyState === 'complete') warmSlides();
  else window.addEventListener('load', warmSlides);

  update();
  startAutoplay();
})();
</script>
<script is:inline type="module">
(function(){let e=document.querySelector(`[data-lang-dropdown]`);if(!e)return;let t=e.querySelector(`.lang-toggle`),n=e.querySelector(`.lang-menu`);function r(){e.classList.remove(`open`),t.setAttribute(`aria-expanded`,`false`)}function i(){e.classList.add(`open`),t.setAttribute(`aria-expanded`,`true`)}t.addEventListener(`click`,function(t){t.stopPropagation(),e.classList.contains(`open`)?r():i()}),document.addEventListener(`click`,function(t){e.contains(t.target)||r()}),document.addEventListener(`keydown`,function(e){e.key===`Escape`&&r()});let a=window.location.hostname,o={"www.meetwhale.com":`Chinese (简体中文)`,"www.whaleai.co.jp":`Japanese (日本語)`,"www.whale.sg":`English (US)`},s=o[a]||`English (US)`;t.querySelector(`span`).textContent=s,n.querySelectorAll(`a`).forEach(function(e){let t=new URL(e.href).hostname;e.setAttribute(`aria-current`,o[t]===s?`true`:`false`)})})();
</script>
<script is:inline>
function md5(input) {
  const te = new TextEncoder();
  const bytes = te.encode(input);
  const len = bytes.length;
  const paddedLen = Math.ceil((len + 9) / 64) * 64;
  const msg = new Uint8Array(paddedLen);
  msg.set(bytes);
  msg[len] = 0x80;
  const view = new DataView(msg.buffer);
  view.setUint32(paddedLen - 8, (len * 8) >>> 0, true);
  view.setUint32(paddedLen - 4, Math.floor(len / 0x20000000), true);

  const T = new Uint32Array(64);
  for (let i = 0; i < 64; i++) T[i] = (Math.floor(Math.abs(Math.sin(i + 1)) * 4294967296)) >>> 0;

  function F(x, y, z) { return (x & y) | (~x & z); }
  function G(x, y, z) { return (x & z) | (y & ~z); }
  function H(x, y, z) { return x ^ y ^ z; }
  function I(x, y, z) { return y ^ (x | ~z); }
  function rotateLeft(x, n) { return (x << n) | (x >>> (32 - n)); }

  let a = 0x67452301, b = 0xEFCDAB89, c = 0x98BADCFE, d = 0x10325476;
  const r1 = [7, 12, 17, 22], r2 = [5, 9, 14, 20], r3 = [4, 11, 16, 23], r4 = [6, 10, 15, 21];

  for (let offset = 0; offset < paddedLen; offset += 64) {
    const M = new Uint32Array(16);
    for (let i = 0; i < 16; i++) M[i] = view.getUint32(offset + i * 4, true);
    let A = a, B = b, C = c, D = d;
    for (let i = 0; i < 16; i++) {
      const f = F(B, C, D); const g = i;
      const tmp = D; D = C; C = B; B = (B + rotateLeft((A + f + T[i] + M[g]) | 0, r1[i % 4])) | 0; A = tmp;
    }
    for (let i = 0; i < 16; i++) {
      const f = G(B, C, D); const g = (5 * i + 1) % 16;
      const tmp = D; D = C; C = B; B = (B + rotateLeft((A + f + T[i + 16] + M[g]) | 0, r2[i % 4])) | 0; A = tmp;
    }
    for (let i = 0; i < 16; i++) {
      const f = H(B, C, D); const g = (3 * i + 5) % 16;
      const tmp = D; D = C; C = B; B = (B + rotateLeft((A + f + T[i + 32] + M[g]) | 0, r3[i % 4])) | 0; A = tmp;
    }
    for (let i = 0; i < 16; i++) {
      const f = I(B, C, D); const g = (7 * i) % 16;
      const tmp = D; D = C; C = B; B = (B + rotateLeft((A + f + T[i + 48] + M[g]) | 0, r4[i % 4])) | 0; A = tmp;
    }
    a = (a + A) | 0; b = (b + B) | 0; c = (c + C) | 0; d = (d + D) | 0;
  }
  function w(v) {
    return (v & 0xff).toString(16).padStart(2, '0') +
      ((v >>> 8) & 0xff).toString(16).padStart(2, '0') +
      ((v >>> 16) & 0xff).toString(16).padStart(2, '0') +
      ((v >>> 24) & 0xff).toString(16).padStart(2, '0');
  }
  return (w(a >>> 0) + w(b >>> 0) + w(c >>> 0) + w(d >>> 0)).toUpperCase();
}
/**
 * Lead modal + form handler for Whale website.
 */
(function () {
  'use strict';

  const EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  const PHONE_RE = /^[0-9\s+\-()]{7,20}$/;

  // Whale Habitat2 CRM open API — production environment.
  const LEAD_ENDPOINT = 'https://habitat2-backend.meetwhale.com/open/leads/acquired/create';
  const APP_ID = '100013';
  const APP_SECRET = '6AcwLKJhxsvsHl2E6lopgtQoIW0n1K3c';
  const LEAD_SOURCE_TYPE = '美区官网留资';

  function generateTimestamp() {
    return String(Math.floor(Date.now() / 1000));
  }

  function buildSignature(bodyJson, appId, appSecret, timestamp) {
    const signStr = bodyJson + '&' + appId + '&' + appSecret + '&' + timestamp;
    return btoa(md5(signStr));
  }

  /**
   * Map the modal's fields onto the Habitat2 CRM acquired-lead contract.
   * The page-level CTA label is kept in sourceDesc; sourceType is pinned to
   * the CRM enum value for US website leads.
   */
  function buildLeadBody(data) {
    const remark = data.remark.trim();
    const extras = [];
    if (data.sourceDesc) extras.push('CTA: ' + data.sourceDesc);
    if (data.customerCountryLabel) extras.push('Country: ' + data.customerCountryLabel);
    if (data.unionCode) extras.push('Ref: ' + data.unionCode);
    const fullRemark = [remark].concat(extras).filter(Boolean).join(' | ');

    return {
      unionCode: data.unionCode,
      customerName: data.customerName.trim(),
      contactName: data.contactName.trim(),
      contactPhone: data.contactPhone.trim(),
      contactEmail: data.contactEmail.trim(),
      customerCountryCode: data.customerCountryCode.trim(),
      customerCountry: data.customerCountryLabel,
      sourceType: LEAD_SOURCE_TYPE,
      sourceDesc: data.sourceDesc || 'spacesight2页留资',
      remark: fullRemark
    };
  }

  const modal = document.getElementById('leadModal');
  const form = modal ? modal.querySelector('[data-lead-form]') : null;
  const errorEl = modal ? modal.querySelector('.lead-error') : null;
  const sourceTypeInput = document.getElementById('leadModalSourceType');
  const sourceDescInput = document.getElementById('leadModalSourceDesc');
  const toast = document.getElementById('leadToast');

  if (!modal || !form) return;

  function generateUnionCode() {
    const date = new Date().toISOString().slice(0, 10).replace(/-/g, '');
    const random = Math.random().toString(36).slice(2, 8).toUpperCase();
    return `WHL-${date}-${random}`;
  }

  function setError(message) {
    if (errorEl) {
      errorEl.textContent = message;
      errorEl.hidden = false;
    }
  }

  function clearError() {
    if (errorEl) {
      errorEl.textContent = '';
      errorEl.hidden = true;
    }
  }

  function resetForm() {
    form.reset();
    const unionInput = form.querySelector('input[name="unionCode"]');
    if (unionInput) unionInput.value = generateUnionCode();
    clearError();
    form.hidden = false;
  }

  function showToast() {
    if (!toast) return;
    toast.hidden = false;
    setTimeout(() => {
      toast.hidden = true;
    }, 4000);
  }

  function openModal(sourceType) {
    if (sourceTypeInput) sourceTypeInput.value = LEAD_SOURCE_TYPE;
    if (sourceDescInput) sourceDescInput.value = sourceType || 'spacesight2页留资';
    resetForm();
    modal.hidden = false;
    document.body.style.overflow = 'hidden';
    const firstInput = form.querySelector('input:not([type="hidden"])');
    if (firstInput) firstInput.focus();
  }

  function closeModal() {
    modal.hidden = true;
    document.body.style.overflow = '';
  }

  function validate(data) {
    if (!data.customerName || data.customerName.trim() === '') {
      return 'Please enter your company name.';
    }
    if (!data.contactName || data.contactName.trim() === '') {
      return 'Please enter your full name.';
    }
    if (!data.customerCountryCode || data.customerCountryCode.trim() === '') {
      return 'Please select your country.';
    }
    if (!data.contactPhone || data.contactPhone.trim() === '') {
      return 'Please enter your phone number.';
    }
    if (!PHONE_RE.test(data.contactPhone)) {
      return 'Please enter a valid phone number.';
    }
    if (!data.contactEmail || data.contactEmail.trim() === '') {
      return 'Please enter your work email.';
    }
    if (!EMAIL_RE.test(data.contactEmail)) {
      return 'Please enter a valid email address.';
    }
    if (!data.sourceType || data.sourceType.trim() === '') {
      return 'Please select a product interest.';
    }
    return null;
  }

  async function submitForm() {
    const submitBtn = form.querySelector('button[type="submit"]');
    const originalLabel = submitBtn ? submitBtn.textContent : 'Submit';

    const formData = new FormData(form);
    const countrySelect = form.querySelector('select[name="customerCountryCode"]');
    const data = {
      unionCode: generateUnionCode(),
      customerName: String(formData.get('customerName') || ''),
      customerCountryCode: String(formData.get('customerCountryCode') || ''),
      customerCountryLabel: countrySelect && countrySelect.selectedOptions[0]
        ? countrySelect.selectedOptions[0].textContent.trim()
        : '',
      contactName: String(formData.get('contactName') || ''),
      contactPhone: String(formData.get('contactPhone') || ''),
      contactEmail: String(formData.get('contactEmail') || ''),
      sourceType: String(formData.get('sourceType') || ''),
      sourceDesc: String(formData.get('sourceDesc') || ''),
      remark: String(formData.get('remark') || '')
    };

    const error = validate(data);
    if (error) {
      setError(error);
      return;
    }

    clearError();

    if (submitBtn) {
      submitBtn.disabled = true;
      submitBtn.textContent = 'Sending...';
    }

    try {
      const bodyJson = JSON.stringify(buildLeadBody(data));
      const timestamp = generateTimestamp();
      const response = await fetch(LEAD_ENDPOINT, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'AppId': APP_ID,
          'timestamp': timestamp,
          'Signature': buildSignature(bodyJson, APP_ID, APP_SECRET, timestamp)
        },
        body: bodyJson
      });

      let result = null;
      try {
        result = await response.json();
      } catch {
        result = null;
      }

      if (submitBtn) {
        submitBtn.disabled = false;
        submitBtn.textContent = originalLabel;
      }

      // Habitat2 CRM contract: { code: 200 | 0, msg, data }
      const accepted = response.ok && result && (result.code === 200 || result.code === 0);

      if (!accepted) {
        setError((result && result.msg) || `Submission failed (${response.status}). Please try again.`);
        return;
      }

      closeModal();
      resetForm();
      showToast();
    } catch (err) {
      if (submitBtn) {
        submitBtn.disabled = false;
        submitBtn.textContent = originalLabel;
      }
      setError('Unable to reach the submission service. Please check your connection and try again.');
    }
  }

  // Open modal triggers
  document.querySelectorAll('[data-open-modal]').forEach((btn) => {
    btn.addEventListener('click', () => {
      openModal(btn.dataset.sourceType);
    });
  });

  // Close modal triggers
  modal.querySelectorAll('[data-close-modal]').forEach((el) => {
    el.addEventListener('click', closeModal);
  });

  // Escape to close
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && !modal.hidden) {
      closeModal();
    }
  });

  // Form submission
  form.addEventListener('submit', (e) => {
    e.preventDefault();
    submitForm();
  });
})();
</script>
<script is:inline type="module">
var e=document.querySelectorAll(`.ss2 [data-reveal]`);if(`IntersectionObserver`in window){let t=new IntersectionObserver(e=>{e.forEach(e=>{e.isIntersecting&&(e.target.classList.add(`is-in`),t.unobserve(e.target))})},{threshold:.12});e.forEach(e=>t.observe(e))}else e.forEach(e=>e.classList.add(`is-in`));var t=document.querySelectorAll(`.ss2-tab`),n=document.querySelectorAll(`[data-tab]`);if(t.length&&`IntersectionObserver`in window){let e=new IntersectionObserver(e=>{e.forEach(e=>{if(!e.isIntersecting)return;let n=e.target.getAttribute(`data-tab`);t.forEach(e=>e.classList.toggle(`is-active`,e.getAttribute(`data-tab-link`)===n))})},{rootMargin:`-40% 0px -55% 0px`});n.forEach(t=>e.observe(t))}var r=window.matchMedia(`(prefers-reduced-motion: reduce)`),i=document.querySelectorAll(`.ss2 video[autoplay]`);function a(){i.forEach(e=>{r.matches?(e.pause(),e.removeAttribute(`autoplay`),e.setAttribute(`controls`,``),e.dataset.pausedByPref=`1`):e.dataset.pausedByPref&&(e.removeAttribute(`controls`),e.play().catch(()=>{}),delete e.dataset.pausedByPref)})}a(),r.addEventListener(`change`,a);
</script>
</body>
</html>
